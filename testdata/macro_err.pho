-- a macro that generates a call to an undefined function: the error
-- must show both the (~evilMacro) call site and the generated code
(macro ~evilMacro () ['fakeFunctionName' 'fakeArgumentName'])
(~evilMacro)
