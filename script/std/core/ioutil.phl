(import 'std/fmt'
        'std/pctl')

(fun print_line ((spread args))
    (pctl.stdout.write (fmt.format (spread args) '\n'))
)

(fun print ((spread args))
    (pctl.stdout.write (fmt.format (spread args)))
)

(fun read_line (prompt)
    (pctl.stdin.read_line prompt)
)

