(goimport ('stdDependencies' dep))
(import 'std/core')

(fun Range (min max)
    (dep.RandomInt min max)
)

(fun Float ()
    (dep.RandomFloat)
)

(fun Shuffle (list) do
    (var shuffled [])
    (while (> list.Size 0) then do
        (var position (Range 0 list.Size))
        (= shuffled (append shuffled list.[position]))
        (= list (list.[: position].Concat list.[(+ position 1) :]))
    )
    shuffled
)