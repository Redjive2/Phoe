(goimport ('stdDependencies' dep))

(import 'std/core'
        'std/fmt')

(fun Log (caller (spread args)) do
    (const data (args.Map
        &(fmt.Format '<' it '>')))
    (dep.DebugLog '[debug.Log @ ' caller '] ' '<n=' args.Size '> : ' (spread data))
)